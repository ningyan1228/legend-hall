import {useMemo,useState} from 'react'; import {Heart,Search} from 'lucide-react'; import type {Person} from '@legend/shared';
export default function Explorer({people,base}:{people:Person[];base:string}){const basePath=base.endsWith('/')?base:`${base}/`;const [q,setQ]=useState('');const [cat,setCat]=useState('all');const [saved,setSaved]=useState<string[]>(()=>{try{return JSON.parse(localStorage.getItem('legend-favorites')||'[]').map((x:any)=>x.slug)}catch{return[]}});const list=useMemo(()=>people.filter(p=>(cat==='all'||p.category===cat)&&[p.nameZh,p.nameEn,p.nationality].join(' ').toLowerCase().includes(q.toLowerCase())),[q,cat,people]);const toggle=(slug:string)=>{const next=saved.includes(slug)?saved.filter(x=>x!==slug):[...saved,slug];setSaved(next);localStorage.setItem('legend-favorites',JSON.stringify(next.map(slug=>({slug,savedAt:new Date().toISOString()}))))};return <section><div className="tools"><label><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索姓名或国家"/></label><div className="filters">{[['all','全部'],['football','足球'],['basketball','篮球'],['film','影视'],['music','音乐']].map(([v,n])=><button className={cat===v?'active':''} onClick={()=>setCat(v)}>{n}</button>)}</div></div><div className="grid">{list.map(p=><article className="card"><a href={`${basePath}people/${p.slug}/`}><div className={`portrait ${p.category}`}>{p.nameZh.slice(0,1)}</div><span className="eyebrow">{p.nationality} · {p.category}</span><h3>{p.nameZh}</h3><p className="en">{p.nameEn}</p><p>{p.summary}</p></a><button className="heart" aria-label="收藏" onClick={()=>toggle(p.slug)}><Heart fill={saved.includes(p.slug)?'currentColor':'none'}/></button></article>)}</div>{!list.length&&<p className="empty">没有找到匹配人物</p>}</section>}
