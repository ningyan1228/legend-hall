services:
  proxy:
    build:
      context: ../..
      dockerfile: apps/proxy/Dockerfile
    restart: unless-stopped
    env_file: .env
    expose: ["3000"]
    networks: [web]
networks:
  web:
    external: true
